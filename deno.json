{
  "tasks": {
    "dev": "deno run --allow-net --allow-read --allow-env --watch main.ts",
    "build": "npm run build",
    "bundle": "deno run --allow-read --allow-write --allow-env scripts/bundle.ts",
    "preview": "deno serve --allow-net --allow-read --port 7654 deploy.ts",
    "deploy": "deno task build && deno task bundle && deno deploy --prod --ignore node_modules --ignore dist --ignore src --ignore scripts --ignore public",
    "deploy-preview": "deno task build && deno task bundle && deno deploy --ignore node_modules --ignore dist --ignore src --ignore scripts --ignore public"
  },
  "deploy": {
    "org": "jhonoryza",
    "app": "visualizer-mesin-tempur-web"
  },
  "imports": {
    "@std/path": "jsr:@std/path@1",
    "@std/fs": "jsr:@std/fs@1"
  }
}

