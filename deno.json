{
  "tasks": {
    "dev": "deno run --allow-net --allow-read --allow-env --watch main.ts",
    "build": "npm run build",
    "bundle": "deno run --allow-read --allow-write --allow-env scripts/bundle.ts",
    "preview": "deno run --allow-net --allow-read --allow-env main.ts",
    "deploy": "deno task build && deno task bundle && deployctl deploy --project=mesin-tempur --prod deploy.ts",
    "deploy-preview": "deno task build && deno task bundle && deployctl deploy --project=mesin-tempur deploy.ts"
  },
  "imports": {
    "@std/path": "jsr:@std/path@1",
    "@std/fs": "jsr:@std/fs@1"
  }
}
