{
  "tasks": {
    "dev": "npm run dev",
    "build": "npm run build",
    "preview": "deno serve --allow-net --allow-read main.ts",
    "deploy": "npm run build && mv index.html index.html.bak && deno deploy --app visualizer-mesin-tempur-web --prod; mv index.html.bak index.html"
  }
}
